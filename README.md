# 🏠 AI 인테리어 디자인 생성기

원룸 사진을 업로드하고 원하는 스타일을 선택하면, AI가 인테리어가 적용된 이미지를 생성하는 데모 애플리케이션입니다.

## 주요 기능

1. **원룸 사진 업로드** - 현재 방 상태를 사진으로 제출
2. **스타일 선택** - 미니멀리스트, 스칸디나비안, 모던, 빈티지, 인더스트리얼 중 선택
3. **AI 이미지 생성** - Google Gemini API로 선택한 스타일이 적용된 인테리어 이미지 생성
4. **결과 비교** - 원본과 생성된 이미지를 나란히 비교

## 기술 스택

- **Backend**: FastAPI
- **AI**: Google Gemini API (gemini-2.5-flash-image-preview)
- **Frontend**: HTML/CSS/JavaScript
- **이미지 처리**: Pillow

## 요구사항

- **Python**: 3.11.9 (권장) 또는 3.8 이상
- **Google Gemini API Key**

## 설치 및 실행

### 1. 의존성 설치

```bash
pip install -r requirements.txt
```

### 2. 환경 변수 설정

`.env.example` 파일을 `.env`로 복사하고 Google Gemini API 키를 설정하세요:

```bash
cp .env.example .env
```

`.env` 파일을 열어서 API 키를 입력:

```
GEMINI_API_KEY=your_actual_api_key_here
```

> **API 키 발급**: https://makersuite.google.com/app/apikey

### 3. 서버 실행

```bash
# 직접 실행
python -m app.main

# 또는 uvicorn 사용
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 4. 브라우저에서 접속

```
http://localhost:8000
```

## API 엔드포인트

### GET `/`
- 메인 웹 페이지

### GET `/api/styles`
- 사용 가능한 인테리어 스타일 목록 반환

### POST `/api/upload`
- 원룸 사진 업로드
- **Request**: multipart/form-data (file)
- **Response**: `{success: true, filename: "...", message: "..."}`

### POST `/api/generate-image`
- 인테리어 스타일이 적용된 이미지 생성
- **Request**: `{image_filename: "...", style_id: "..."}`
- **Response**: `{success: true, generated_image: "...", original_image: "...", style: "..."}`

### GET `/api/images/{filename}`
- 업로드되거나 생성된 이미지 파일 반환

### GET `/health`
- 헬스 체크 및 API 키 설정 확인

## 프로젝트 구조

```
backend/
├── app/
│   ├── __init__.py
│   ├── main.py                 # FastAPI 메인 애플리케이션
│   ├── models/
│   │   ├── __init__.py
│   │   └── schemas.py          # Pydantic 데이터 모델
│   ├── routes/
│   │   ├── __init__.py
│   │   └── design.py           # API 라우트
│   └── services/
│       ├── __init__.py
│       └── gemini_service.py   # Gemini API 연동
├── templates/
│   └── index.html              # 메인 웹 페이지
├── static/                     # 정적 파일 (CSS, JS)
├── uploads/                    # 업로드된 이미지 저장
├── requirements.txt
├── .env.example
└── README.md
```

## 사용 방법

1. 웹 페이지 접속
2. **Step 1**: 원룸 사진 업로드 (드래그 앤 드롭 또는 클릭)
3. **Step 2**: 원하는 인테리어 스타일 선택
4. **"인테리어 이미지 생성하기"** 버튼 클릭
5. AI가 생성한 결과 확인:
   - 원본 이미지와 생성된 이미지 비교
   - 선택한 스타일이 적용된 인테리어 확인

**참고**: 이미지 생성에는 약 30초 정도 소요될 수 있습니다.

## 주의사항

- Google Gemini API 키가 필요합니다
- 업로드 가능한 이미지 형식: JPG, PNG, WEBP
- DB나 로그인 기능은 포함되어 있지 않습니다 (데모 목적)
- 업로드된 파일은 `uploads/` 디렉토리에 저장됩니다

## 라이센스

MIT License
